{
    "$schema": "https://json-schema.org/draft/2020-12/schema#",
    "$id": "https://pleiades.stoa.org/linkedplaces/schema/periods.json",
    "title": "periods",
    "type": "array",
    "minItems": 1,
    "items": {
        "$ref": "#/$defs/period"
    },
    "$defs": {
        "period": {
            "type": "object",
            "required": [
                "name",
                "@id"
            ],
            "properties": {
                "name": {
                    "type": "string",
                    "pattern": "^.*[^\\s]+.*$"
                },
                "@id": {
                    "$ref": "#/$defs/uri"
                }
            }
        },
        "uri": {
            "type": "string",
            "pattern": "^(.+:.+|https?:\\\\.+)$"
        }
    }
}